{
    # E-mail para Let’s Encrypt
    email you@example.com
}

# Bloco principal em produção
example.com {
    root * /app/public
    encode zstd br gzip

    # Sequência de handlers
    route {
        # PHP via FrankenPHP/Swoole
        php_server

        # Arquivos estáticos
        file_server
    }
}
